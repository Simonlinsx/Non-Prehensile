diff --git a/systems/controllers/sampling_based_c3_controller.cc b/systems/controllers/sampling_based_c3_controller.cc
--- a/systems/controllers/sampling_based_c3_controller.cc
+++ b/systems/controllers/sampling_based_c3_controller.cc
@@ -1975,8 +1975,10 @@
       throw std::runtime_error(
           "Semantic guard object mesh count exceeds object state count");
     }
+    // Drake state quaternions are wxyz; Eigen's vector constructor is xyzw.
     Quaterniond object_quaternion(
-        x_lcs.segment<4>(quaternion_index));
+        x_lcs(quaternion_index), x_lcs(quaternion_index + 1),
+        x_lcs(quaternion_index + 2), x_lcs(quaternion_index + 3));
     if (object_quaternion.norm() < 1.0e-12) {
       // A malformed predicted orientation must fail closed.
       if (closest_object_index != nullptr) {
diff --git a/examples/sampling_c3/semantic_c1_trajectory_guard.cc b/examples/sampling_c3/semantic_c1_trajectory_guard.cc
--- a/examples/sampling_c3/semantic_c1_trajectory_guard.cc
+++ b/examples/sampling_c3/semantic_c1_trajectory_guard.cc
@@ -204,7 +204,9 @@
       return std::numeric_limits<double>::quiet_NaN();
     }
     const Eigen::VectorXd positions = object_state->GetPositions();
-    Eigen::Quaterniond quaternion(positions.head<4>());
+    // Drake state quaternions are wxyz; Eigen's vector constructor is xyzw.
+    Eigen::Quaterniond quaternion(
+        positions(0), positions(1), positions(2), positions(3));
     if (quaternion.norm() < 1.0e-12) {
       return std::numeric_limits<double>::quiet_NaN();
     }
